# Frontend production build — set in Vercel project settings → Environment Variables
#
# Leave VITE_API_URL unset so the app calls same-origin /api/* (proxied to Render via vercel.json).
# Only set this for a cross-origin API setup (not recommended on mobile — third-party cookies are blocked).
# VITE_API_URL=
